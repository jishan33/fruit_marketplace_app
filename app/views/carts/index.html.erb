<h1>Cart</h1>

<ul>
  <% @cart.each do |product| %>
    <li><%= product.title %> | <%= number_to_currency(product.price) %></li> | <%= product.id %> | <%= link_to 'Delete', carts_path + "/#{product.id}", method: :delete, class: 'btn btn-danger sm', data: { confirm: "Really delete #{product.title}?"} %>
  <% end %>
</ul>

<%= link_to "Purchase All Now 💵", "", id: "stripe" %>
<%= link_to "See Products", products_path, class: 'btn btn-primary sm' %>
<%= javascript_pack_tag "stripe" %>

